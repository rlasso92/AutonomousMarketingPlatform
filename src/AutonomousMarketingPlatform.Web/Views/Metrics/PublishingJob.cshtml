@model AutonomousMarketingPlatform.Application.DTOs.PublishingJobMetricsSummaryDto
@{
    ViewData["Title"] = "Métricas de Publicación";
}

<div class="content-wrapper">
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1><i class="fas fa-chart-line mr-2"></i>Métricas de Publicación</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="/">Inicio</a></li>
                        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Publishing")">Publicaciones</a></li>
                        <li class="breadcrumb-item active">Métricas</li>
                    </ol>
                </div>
            </div>
        </div>
    </section>

    <section class="content">
        <div class="container-fluid">
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    @TempData["SuccessMessage"]
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            }

            <!-- Información del post -->
            <div class="card mb-3">
                <div class="card-header">
                    <h3 class="card-title">Información del Post</h3>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-3">Canal:</dt>
                        <dd class="col-sm-9"><span class="badge badge-primary">@Model.Channel</span></dd>
                        <dt class="col-sm-3">Fecha de publicación:</dt>
                        <dd class="col-sm-9">@(Model.PublishedDate?.ToString("dd/MM/yyyy HH:mm") ?? "N/A")</dd>
                        <dt class="col-sm-3">Contenido:</dt>
                        <dd class="col-sm-9">@(Model.Content?.Length > 100 ? Model.Content.Substring(0, 100) + "..." : Model.Content ?? "N/A")</dd>
                    </dl>
                </div>
            </div>

            <!-- Resumen -->
            <div class="row">
                <div class="col-lg-3 col-6">
                    <div class="small-box bg-info">
                        <div class="inner">
                            <h3>@Model.TotalImpressions.ToString("N0")</h3>
                            <p>Impresiones</p>
                        </div>
                        <div class="icon">
                            <i class="fas fa-eye"></i>
                        </div>
                    </div>
                </div>
                <div class="col-lg-3 col-6">
                    <div class="small-box bg-success">
                        <div class="inner">
                            <h3>@Model.TotalClicks.ToString("N0")</h3>
                            <p>Clics</p>
                        </div>
                        <div class="icon">
                            <i class="fas fa-mouse-pointer"></i>
                        </div>
                    </div>
                </div>
                <div class="col-lg-3 col-6">
                    <div class="small-box bg-warning">
                        <div class="inner">
                            <h3>@Model.TotalEngagement.ToString("N0")</h3>
                            <p>Engagement</p>
                        </div>
                        <div class="icon">
                            <i class="fas fa-heart"></i>
                        </div>
                    </div>
                </div>
                <div class="col-lg-3 col-6">
                    <div class="small-box bg-danger">
                        <div class="inner">
                            <h3>@Model.DaysWithMetrics</h3>
                            <p>Días con Métricas</p>
                        </div>
                        <div class="icon">
                            <i class="fas fa-calendar"></i>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Tasas -->
            <div class="row">
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title">Tasas de Rendimiento</h3>
                        </div>
                        <div class="card-body">
                            <dl class="row">
                                <dt class="col-sm-6">Click-Through Rate (CTR):</dt>
                                <dd class="col-sm-6">
                                    <strong>@(Model.AverageClickThroughRate?.ToString("F2") ?? "N/A")%</strong>
                                </dd>
                                <dt class="col-sm-6">Engagement Rate:</dt>
                                <dd class="col-sm-6">
                                    <strong>@(Model.AverageEngagementRate?.ToString("F2") ?? "N/A")%</strong>
                                </dd>
                            </dl>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title">Desglose de Engagement</h3>
                        </div>
                        <div class="card-body">
                            <dl class="row">
                                <dt class="col-sm-6">Likes:</dt>
                                <dd class="col-sm-6"><strong>@Model.TotalLikes.ToString("N0")</strong></dd>
                                <dt class="col-sm-6">Comentarios:</dt>
                                <dd class="col-sm-6"><strong>@Model.TotalComments.ToString("N0")</strong></dd>
                                <dt class="col-sm-6">Compartidos:</dt>
                                <dd class="col-sm-6"><strong>@Model.TotalShares.ToString("N0")</strong></dd>
                            </dl>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Métricas diarias -->
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Métricas Diarias</h3>
                    <div class="card-tools">
                        <a href="@Url.Action("RegisterPublishingJob", new { publishingJobId = Model.PublishingJobId })" class="btn btn-primary btn-sm">
                            <i class="fas fa-plus mr-1"></i>Registrar Métricas
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    @if (Model.DailyMetrics.Any())
                    {
                        <table class="table table-bordered table-striped">
                            <thead>
                                <tr>
                                    <th>Fecha</th>
                                    <th>Impresiones</th>
                                    <th>Clics</th>
                                    <th>Engagement</th>
                                    <th>Likes</th>
                                    <th>Comentarios</th>
                                    <th>Compartidos</th>
                                    <th>CTR</th>
                                    <th>Eng. Rate</th>
                                    <th>Fuente</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var metric in Model.DailyMetrics.OrderByDescending(m => m.MetricDate))
                                {
                                    <tr>
                                        <td>@metric.MetricDate.ToString("dd/MM/yyyy")</td>
                                        <td>@metric.Impressions.ToString("N0")</td>
                                        <td>@metric.Clicks.ToString("N0")</td>
                                        <td>@metric.Engagement.ToString("N0")</td>
                                        <td>@metric.Likes.ToString("N0")</td>
                                        <td>@metric.Comments.ToString("N0")</td>
                                        <td>@metric.Shares.ToString("N0")</td>
                                        <td>@(metric.ClickThroughRate?.ToString("F2") ?? "N/A")%</td>
                                        <td>@(metric.EngagementRate?.ToString("F2") ?? "N/A")%</td>
                                        <td>
                                            <span class="badge @(metric.IsManualEntry ? "badge-warning" : "badge-info")">
                                                @(metric.Source ?? "N/A")
                                            </span>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle mr-2"></i>
                            No hay métricas registradas para esta publicación. 
                            <a href="@Url.Action("RegisterPublishingJob", new { publishingJobId = Model.PublishingJobId })">Registrar métricas manualmente</a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>
</div>

